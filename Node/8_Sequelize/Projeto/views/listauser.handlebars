<h1>Lista de usuários cadastrados</h1>

<div class="container">
    {{#each users}}
        <div class="item">
            <p>{{this.name}}</p>
            <a href="/user/{{this.id}}">Ver detalhes</a>
        </div>
    {{/each}}
</div>